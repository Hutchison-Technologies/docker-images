# gcloud-node22-npm

This image is built with Node `v22.22.1`

## Build and push process

```docker
docker build -t hutchisont/gcloud-node22-npm .
```

Once tagged, push to dockerhub via:

```docker
docker push hutchisont/gcloud-node22-npm:latest
```
